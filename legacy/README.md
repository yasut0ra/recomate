# Legacy Code

このディレクトリには、現行の `api/` + `ui/` 構成へ統一する前の旧コードを退避しています。

含まれるもの:

- `legacy/node-express/`
  - 旧 Node/Express バックエンド試作
- `legacy/electron/`
  - 旧 Electron エントリ

現行の起動導線では使いません。通常はルートの `npm run dev` または `npm run dev:web` を使ってください。
